# Catalyst Table Creator

1. pip install -r requirements.txt
2. playwright install
3. python create_tables.py

The current package contains the framework plus the first schemas.
Extend `schemas.py` with the remaining tables from your specification.
